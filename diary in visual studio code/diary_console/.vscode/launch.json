{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run active C/C++ file (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "preLaunchTask": "Build active C/C++ file with MSVC",
            "console": "externalTerminal"
        }
    ]
}